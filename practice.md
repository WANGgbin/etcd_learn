描述 etcd 实践相关内容。

# 使用了 raft，etcd 就一定能保证数据一致性吗

不一定。即使在 raft 层达成了一致，在 apply 到状态机的时候仍然有可能失败，进而导致节点之间数据不一致。


# 分布式锁

实现分布式锁有多种方式，比如 redis、zookeeper、etcd。那么这几种方式区别是什么？如何选型？

## redis

### 实现

- 加锁
    - 使用 redis set lock_key id nx ex
    - 加锁成功，需要不断进行锁续期操作
    - 加锁失败，需要不断轮询判断能否重新加锁

- 释放锁

    删除 key

### 特性

- 优点：

    性能好。

- 缺点：

    - 无法实现阻塞锁。当占有锁失败的时候，需要不断的轮询去判断能否占有锁，存在大量不必要的网络调用。
    - 无法实现公平锁。当锁释放的时候，各个节点重新加锁，结果随机，无法保证公平锁。
    - 锁丢失。因为 redis 节点之间复制本来就是异步的。如果主节点挂了，锁信息还没有同步到从节点，锁信息就丢失了，进而导致其他业务进程
    可以占有锁，从而无法保证互斥性。

## etcd

### 实现

具体实现可以参考 etcd: client/v3/concurrency/mutex.go

- 加锁：

每个业务进程(线程)创建一个公共前缀的 key，然后判断自己创建的 key 的 createRevision 是不是相同前缀的所有 key 中最小的，
如果是，则加锁成功。否则，需要 watch 比自己 createRevision 小的其他公共前缀的 key，直到这些 key 全部删除，则加锁成功。

- 释放锁：

删除 key 即可。

### 优缺点

- 优点

    - 强一致性，不会出现锁丢失的问题。
    - 通过 watch 机制，可以实现锁阻塞机制。比如加锁失败，只需要 watch 目标 key 信息，不需要轮询。
    - 实现公平锁。先尝试加锁的业务进程在锁释放的时候先获取到锁。
    - 避免惊群效应：锁释放，重新激活所有的加锁进程。而是只唤醒下一个 key.

- 缺点

    因为是强一致性，所以相比于 redis 性能差点。

## zookeeper

zookeeper 实现分布式锁思路与 etcd 类似。整体思路如下；

- 加锁

    加锁进程在锁节点下创建一个临时顺序节点，zookeeper 会为顺序节点分配一个递增的 seq。如果创建的节点是锁节点下最小的节点，则加锁成功。
否则加锁失败，同时 watch 上一个节点，当上一个节点删除的时候，唤醒当前进程，重新判断。

- 释放锁

    删除节点即可。

zookeeper 的优缺点与 etcd 基本类似。

# 其他使用场景

除了实现分布式锁外，还可以实现服务注册/发现、leader 选举 等。